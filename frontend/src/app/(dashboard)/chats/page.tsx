"use client";

/**
 * [INPUT]: 依赖 chats/layout.tsx 承载真正的 DashboardApp
 * [OUTPUT]: 对外提供 /chats 根路由占位页
 * [POS]: chats 动态布局下的空页面，只负责让 URL 命中 layout
 * [PROTOCOL]: 变更时更新此头部，然后检查 README.md
 */

export default function ChatsPage() {
  return null;
}
