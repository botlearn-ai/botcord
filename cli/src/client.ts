// Re-exported from @botcord/protocol-core so existing CLI imports keep working.
// New code should import directly from @botcord/protocol-core.
export { BotCordClient } from "@botcord/protocol-core";
export type { BotCordClientConfig } from "@botcord/protocol-core";
